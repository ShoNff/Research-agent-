output/
*.pyc
__pycache__/
.env
*.egg-info/
dist/
build/
.venv/

# Per-environment retrospective watermark (not shared)
.claude/retrospective-state.json

# Per-environment hook firing log (not shared)
.claude/logs/

# Edition run logs (the edition.json/md are committed; the traces are not)
editions/*/logs/
